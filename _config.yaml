url: "https://www.tuned.org.uk"

plugins:
  - jekyll-feed
  - jekyll-sitemap

defaults:
  -
    scope:
      path: ""
      type: posts
    values:
      layout: blog_default
      permalink: /posts/:title/
